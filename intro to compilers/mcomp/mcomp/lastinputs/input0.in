PROGRAM h
     a  FIXED;
     PROCEDURE f ( m FIXED IDENTICAL, f  FIXED)	{
	 m = m * 2;
 	 a = x - m;
      }
     PROCEDURE g ( q FIXED IDENTICAL, w  FIXED)	{
	 q = q * 2;
 	 a = w - q;
      }
{
      f(a, a);
}
